package td;

public class LearningSudo {
    /*
     * weights[time+1] + weights[time] = learningRate * (Output[time+1] -
     * Output[time]) * for(int j = 1; j <=i; j++){ temp += (Math.pow(lambda,
     * time-j) * Output[j] * weightGradien) } }
     */
}